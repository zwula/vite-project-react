import { Layout } from 'antd'
import './index.less'

const classPrefix = 'footer'
const Footer = () => {
	return <Layout.Footer className={classPrefix}>Footer</Layout.Footer>
}

export default Footer
